{
    "name": "aws-typescript",
    "scripts": {
        "build": "sudo docker build -t pulumi .",
        "pulumi": "sudo docker run --env-file=.env -it docker-pulumi"
    },
    "devDependencies": {
        "@types/node": "latest"
    },
    "dependencies": {
        "@pulumi/pulumi": "latest",
        "@pulumi/aws": "latest",
        "@pulumi/awsx": "latest"
    }
}
